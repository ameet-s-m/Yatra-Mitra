// Importing necessary packages
import 'package:flutter/material.dart';

class LoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Yatra Mitra'),
      ),
      body: Center(
        child: Text('Welcome to Yatra Mitra!'),
      ),
    );
  }
}